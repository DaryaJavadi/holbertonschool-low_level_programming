/**
 *swap_int - updates the value to 98
 *@a: input
 *@b: input
 *
 */
#include <stdio.h>
#include "main.h"
int _strlen(char *s)
{
char str[];
printf("write your string: %c\n,str")
int len = strlen(str);
printf("Length of the string is: %d\n", len)
	return 0;
}
